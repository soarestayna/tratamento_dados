#  Funções Pandas

# dropna()        | Remove valores nulos ('NaN') da tabela
# fillna(valor)   | Preenche os 'NaN' com o valor escolhido
# apply(func)     | Aplica uma função a cada elemento
# groupby(coluna) | Agrupa os dados por categorias
# value_counts()  | Conta quantas vezes cada valor aparece
# describe()      | Mostra estatísticas gerais dos dados

# inplace=True: Modifica o DataFrame original, remove a coluna diretamente
# inplace=False: A função retorna uma cópia modificada, cria nova versão, preserva o df original


